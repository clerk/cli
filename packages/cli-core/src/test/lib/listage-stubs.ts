import { filterChoices, Separator } from "../../lib/listage.ts";

export const listageStubs = {
  select: async () => undefined,
  search: async () => undefined,
  filePath: async () => "",
  filterChoices,
  Separator,
};
