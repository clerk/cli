---
"clerk": minor
---

Add `clerk users list` with pagination, query search, repeatable identifier filters (`--email-address`, `--phone-number`, `--username`, `--user-id`, `--external-id`), and `--order-by` covering Clerk's common user ordering fields with optional `+`/`-` prefixes.
