---
"clerk": minor
---

Collect anonymous usage telemetry (command name, flag names, duration, outcome, a random machine identifier, and — when a project is linked — the app and workspace IDs) to help improve the CLI; opt out with `CLERK_TELEMETRY_DISABLED=1` or `DO_NOT_TRACK=1`.
