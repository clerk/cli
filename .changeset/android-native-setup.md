---
"clerk": minor
---

Automate Android setup in `clerk init`: enable Native API, register the application and signing fingerprints, install the SDK, and initialize Clerk in Kotlin. Add a read-only dry run and explicit module/application ID options for variant-aware setup.
