---
"clerk": minor
---

Add native macOS support to `clerk init` and `clerk doctor`, including ClerkKit and optional AuthView setup, App Sandbox outgoing network access, Native Application reconciliation, Sign in with Apple configuration, and macOS Xcode build verification. Multiplatform targets are validated across every supported Apple platform and are automated only when they share one Clerk Swift application root and one case-insensitive Bundle ID.
