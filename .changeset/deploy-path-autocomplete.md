---
"clerk": minor
---

Add filesystem autocomplete to the file-path prompts in `clerk deploy` (the Google OAuth JSON import and Apple `.p8` private key). Type part of a path to filter matching entries, pick a directory to drill into it, or pick a file to submit. A leading `~` expands to your home directory.
